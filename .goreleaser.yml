version: 2

before:
  hooks:
    - go mod download
    - go generate ./...
builds:
  - id: default
    env:
      - CGO_ENABLED=1
    ldflags:
      - "-s"
      - "-w"
      - "-X github.com/FMotalleb/go-tools/git.Version={{.Version}}"
      - "-X github.com/FMotalleb/go-tools/git.Commit={{.ShortCommit}}"
      - "-X github.com/FMotalleb/go-tools/git.Date={{.Date}}"
      - "-X github.com/FMotalleb/go-tools/git.Branch={{.Branch}}"
    goos:
      - darwin
      - linux
      # - windows
    goarch:
      - amd64

archives:
  - format_overrides:
      - goos: windows
        formats: 
          - zip
dockers:
  - image_templates:
    - "ghcr.io/fmotalleb/north_outage:latest"
    - "ghcr.io/fmotalleb/north_outage:{{ .Major }}"
    - "ghcr.io/fmotalleb/north_outage:{{ .Major }}.{{ .Minor }}"
    - "ghcr.io/fmotalleb/north_outage:{{ .Major }}.{{ .Minor }}.{{ .Patch }}"
    build_flag_templates:
    - "--pull"
    # OCI annotations: https://github.com/opencontainers/image-spec/blob/main/annotations.md
    - "--label=org.opencontainers.image.created={{.Date}}"
    - "--label=org.opencontainers.image.name={{.ProjectName}}"
    - "--label=org.opencontainers.image.revision={{.FullCommit}}"
    - "--label=org.opencontainers.image.version={{.Version}}"
    - "--label=org.opencontainers.image.source={{.GitURL}}"
release:
  github:
    owner: FMotalleb
    name: north_outage
  name_template: "{{.Version}}"
  prerelease: auto
  mode: append
changelog:
  disable: true
