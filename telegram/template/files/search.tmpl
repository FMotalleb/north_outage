🤖 موارد یافت شده:

{{- range $i, $e := .results }}
<blockquote expandable>• [{{$i | add1 }}] <b>{{ $e.City }}</b> {{ $e.Start | jFormat "%w %Y/%m/%d %B" }}
	{{ $e.StartClock }}{{ $e.Start | jFormat "%R" }} تا {{ $e.EndClock }}{{ $e.End | jFormat "%R" }}
  {{ $e.Address }}
</blockquote>
{{- end }}

📌 توضیحات:  
این لیست شامل همه مواردی است که مطابق جستجوی شما یافت شده است. برای مشاهده جزئیات بیشتر، از دستورات بات استفاده کنید.